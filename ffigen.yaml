# Run with `dart run ffigen --config ffigen.yaml`.
name: TracingGameBindings
description: |
  Bindings for `src/tracing_game.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/tracing_game_bindings_generated.dart'
headers:
  entry-points:
    - 'src/tracing_game.h'
  include-directives:
    - 'src/tracing_game.h'
preamble: |
  // ignore_for_file: always_specify_types
  // ignore_for_file: camel_case_types
  // ignore_for_file: non_constant_identifier_names
comments:
  style: any
  length: full
